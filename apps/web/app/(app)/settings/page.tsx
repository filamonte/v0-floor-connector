import { AppModulePlaceholder } from "@/components/app-module-placeholder";

export default function SettingsPage() {
  return (
    <AppModulePlaceholder
      eyebrow="Settings Module"
      title="Settings"
      description="This protected area will become the account, organization, and future team-management settings surface. It is intentionally minimal for now so the app shell can be verified without jumping ahead into feature work."
    />
  );
}
