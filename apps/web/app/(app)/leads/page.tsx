import { AppModulePlaceholder } from "@/components/app-module-placeholder";

export default function LeadsPage() {
  return (
    <AppModulePlaceholder
      eyebrow="Leads Module"
      title="Leads"
      description="This area will become the intake and opportunity workspace for incoming flooring jobs, qualification, and early sales follow-up. It is intentionally lightweight right now so the shared dashboard layout can be used and tested without jumping ahead into full CRM workflow implementation."
    />
  );
}
